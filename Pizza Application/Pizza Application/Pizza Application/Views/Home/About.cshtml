@model IEnumerable<Pizza_Application.Models.OrderViewModel>
@{
    ViewBag.Title = "Orders";
}
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 60%;
        }

        td, th {
            border: 1px solid;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<h2>@ViewBag.Message</h2>

<table>
    <tr>
        <th>Name</th>
        <th>Phone Number</th>
        <th>Delivery Time</th>
        <th>Pizzas</th>
    </tr>
    @foreach (var item in @Model.Reverse())
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.PhoneNumber</td>
            <td>@item.DateTime</td>
            <td>@item.NumOfPizzas</td>
        </tr>
    }
</table>
